<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Immunify" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_yellow</item>
        <item name="colorPrimaryVariant">@color/shade_yellow</item>
        <item name="colorOnPrimary">@color/background_white </item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_pink</item>
        <item name="colorSecondaryVariant">@color/shade_pink</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Immunify.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Immunify.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Immunify.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>